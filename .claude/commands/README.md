# Claude Code Commands

These commands are meant to be run from this repository after cloning it.

```text
/repo-onboarding /absolute/path/to/target-repo
/discovery /absolute/path/to/target-repo
/onboarding /absolute/path/to/target-repo
```

They call the local Python CLI with `PYTHONPATH=src`, so editable install is not required for command testing.
